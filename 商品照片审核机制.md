# 叮咚号卡商品照片上传与审核功能设计方案
## 功能概述


### 业务场景
- **商品启用照片要求**：管理员为特定商品开启"需要上传身份验证照片"功能
- **用户下单上传**：用户在H5页面下单时，根据商品配置上传身份证正面、反面、免冠照片等
- **代理商维护**：下单成功后，代理商可以管理用户的照片信息，确保照片质量
- **管理员审核**：管理员审核照片通过后，订单继续正常流程；审核拒绝则退回代理商重新处理


### 2. 订单表扩展 (t_order)

基于现有订单表，直接扩展照片相关字段，避免创建额外的关联表：

```sql
-- 基于现有订单表，新增照片相关字段
ALTER TABLE t_order ADD COLUMN `id_card_front_url` VARCHAR(500) DEFAULT '' COMMENT '身份证正面照片URL';
ALTER TABLE t_order ADD COLUMN `id_card_back_url` VARCHAR(500) DEFAULT '' COMMENT '身份证反面照片URL';
ALTER TABLE t_order ADD COLUMN `person_photo_url` VARCHAR(500) DEFAULT '' COMMENT '免冠照片URL';
ALTER TABLE t_order ADD COLUMN `custom_photo_url` VARCHAR(500) DEFAULT '' COMMENT '自定义照片URL';

ALTER TABLE t_order ADD COLUMN `photo_status` TINYINT(1) DEFAULT 0 COMMENT '照片审核状态 0 无需审核 1 待上传照片 2 代理商待提交 3 管理员待审核 4 审核通过 5 审核拒绝';
ALTER TABLE t_order ADD COLUMN `photo_upload_time` BIGINT(20) DEFAULT '' COMMENT '照片上传时间';
ALTER TABLE t_order ADD COLUMN `photo_audit_user_id` BIGINT(20) DEFAULT '' COMMENT '照片审核员ID';
ALTER TABLE t_order ADD COLUMN `photo_audit_time` BIGINT(20) DEFAULT '' COMMENT '照片审核时间';
ALTER TABLE t_order ADD COLUMN `photo_audit_remark` VARCHAR(500) DEFAULT '' COMMENT '照片审核备注';
```

### 3. 审核日志机制

利用现有的订单状态变更记录机制，照片审核的状态变更将记录在现有的订单操作日志中，无需单独创建照片审核日志表。

## 二、基于现有订单状态的设计

### 1. 订单状态扩展

基于现有的 `OrderEnum`，照片审核作为订单状态的一个子状态：

```java
// 现有订单状态
public enum OrderEnum {
    INVALID(-1, "订单失败"),
    CREATE(0, "订单预创建"),
    APPLY(1, "订单申请成功"),
    SHIPPED(2, "订单已发货"),
    RECEIVED(3, "订单已签收"),
    ACTIVATED(4, "订单已激活");

    // 新增照片审核状态
    // 当商品需要照片审核时，订单状态为 APPLY(1) "订单申请成功"
    // 同时 photo_status 表示照片审核进度
}

// 照片审核状态枚举
public enum PhotoAuditEnum {
    NONE(0, "无需审核"),
    UPLOAD_PENDING(1, "待上传照片"),     // 用户待上传
    AGENT_PENDING(2, "代理商待提交"),    // 代理商待提交审核
    ADMIN_PENDING(3, "管理员待审核"),    // 管理员待审核
    APPROVED(4, "审核通过"),
    REJECTED(5, "审核拒绝");

    private Integer status;
    private String message;
}
```

### 2. 状态流转逻辑

**需要照片审核的商品完整流程：**

```
1. H5用户下单 → Order.buildOrderFromGeneral() → CREATE(0) + photo_status=UPLOAD_PENDING(1)
   ↓
2. 用户上传照片 → 前端上传到订单表 → photo_status=AGENT_PENDING(2)
   ↓
3. 代理商检查并提交审核 → photo_status=ADMIN_PENDING(3)
   ↓
4. 管理员审核：
   - 通过 → photo_status=APPROVED(4) → 订单继续正常流程（向上游API提交）
   - 拒绝 → photo_status=REJECTED(5) + 审核备注 → 返回代理商重新处理
   ↓
5. 代理商重新处理 → 重新上传照片 → photo_status=AGENT_PENDING(2) → 重新进入审核流程
```

**无需照片审核的商品：**
```
用户下单 → CREATE(0) → APPLY(1) → 直接进入正常流程（无需照片审核）
```

**导入订单流程：**
```
管理员批量导入 → ImportServiceImpl.importOrderListSubmit()
   ↓
异步调用相同下单接口 → 后续流程同直接下单