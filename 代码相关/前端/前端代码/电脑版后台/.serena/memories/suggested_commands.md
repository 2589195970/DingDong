# 常用命令
- `npm install`：安装依赖。
- `npm run dev`：本地开发（热更新）。
- `npm run build:prod`：生产构建。
- `npm run build:stage`：staging 构建。
- `npm run lint`：执行 ESLint（pre-commit 同样走 lint-staged）。
- `vue-cli-service serve|build`：底层脚本，可直接调用。
- `rg <pattern>` / `rg --files`：快速搜索（推荐工具链规定）。