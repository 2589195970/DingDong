<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.console.mapper.AgentProductMapper">

    <insert id="insertBatch">
        INSERT INTO t_agent_product (
            parent_product_code,
            parent_product_name,
            parent_agent_code,
            agent_code,
            agent_name,
            is_all_agent,
            product_status,
            product_commission,
            revenue_product_commission,
            distribution_product_commission,
            product_sort,
            create_time,
            update_time,
            product_qrcode_map
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.parentProductCode},
                #{item.parentProductName},
                #{item.parentAgentCode},
                #{item.agentCode},
                #{item.agentName},
                #{item.isAllAgent},
                #{item.productStatus},
                #{item.productCommission},
                #{item.revenueProductCommission},
                #{item.distributionProductCommission},
                #{item.productSort},
                #{item.createTime},
                #{item.updateTime},
                #{item.productQrcodeMap}
            )
        </foreach>
    </insert>

</mapper>
